{
  "name": "care4u",
  "version": "1.0.0",
  "description": "This application try to help with people who have communication difficulties, to make then express themselvies better.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html -p 3000 --open",
    "build": "parcel build index.html -d build --public-url ./"
  },
  "proxy": "http://localhost:5000",
  "author": "Will Chen",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.8.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "parcel-bundler": "^1.6.2"
  }
}
